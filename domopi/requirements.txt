# --- Serveur Web / API ---
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9

# --- Validation / configuration ---
pydantic>=2.7
pydantic-settings>=2.3

# --- Base de donnees ---
SQLAlchemy>=2.0

# --- Securite ---
bcrypt>=4.1
PyJWT>=2.8

# --- HTTP client (Hue, decouverte) ---
httpx>=0.27

# --- Decouverte reseau (mDNS pour Hue / Matter) ---
zeroconf>=0.132

# --- Planification horaire ---
APScheduler>=3.10

# --- HomeKit (HAP - HomeKit Accessory Protocol) ---
HAP-python>=4.9

# --- Matter (client officiel python-matter-server) ---
# Le serveur Matter tourne comme service dedie (voir scripts/install.sh).
python-matter-server>=6.0 ; platform_machine == "aarch64"

# --- RF 433 MHz / Somfy RTS (uniquement sur Raspberry Pi) ---
rpi-rf>=0.9.7 ; platform_machine == "aarch64"
pigpio>=1.78 ; platform_machine == "aarch64"
